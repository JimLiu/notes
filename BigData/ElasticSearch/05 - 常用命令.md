## 常用命令
### 增加字段
```Bash
curl -X POST "${ip}:${port}/${index}/_mapping/${type}" -H 'Content-Type: application/json' -d'
{
  "properties": {
    "${columnName}": { 
      "type":"integer"
    }
  }
}'
```